[project]
name = "wrapfast"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
license = "0BSD"
license-files = ["LICENSE"]
dependencies = []

[project.optional-dependencies]
examples = ["pydantic", "requests"]
